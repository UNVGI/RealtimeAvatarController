T7-1 Provider Registry 未初期化時の HelpBox 実装

## 検証内容

### 1. T7-1 完了条件の再確認
- Registry に Provider が登録されていない (`_providerTypeIds.Length == 0`) 状態で Inspector を開いた際に、
  Warning HelpBox と Provider Type ID 手入力フィールドが表示されることを保証する。

### 2. 既存実装の確認 (T3-1 で実装済み)
ファイル: `Packages/com.cysharp.realtimeavatarcontroller/Samples~/UI/Editor/SlotSettingsEditor.cs`
DrawAvatarProviderSection() 内 (L88-L101) に T7-1 で指定されたコードスニペットと
完全一致する実装が既に存在する:

```csharp
if (_providerTypeIds.Length == 0)
{
    EditorGUILayout.HelpBox(
        "Registry に Provider が未登録です。\n[InitializeOnLoadMethod] が実行されているか確認してください。",
        MessageType.Warning);
    EditorGUILayout.PropertyField(typeIdProp, new GUIContent("Provider Type ID (手入力)"));
}
else
{
    // ドロップダウン描画
}
```

検証項目:
- [x] `_providerTypeIds.Length == 0` の分岐が存在する
- [x] HelpBox メッセージがタスク指定の文字列と一致する
  ("Registry に Provider が未登録です。\n[InitializeOnLoadMethod] が実行されているか確認してください。")
- [x] `MessageType.Warning` を使用している
- [x] 手入力フォールバック `EditorGUILayout.PropertyField(typeIdProp, new GUIContent("Provider Type ID (手入力)"))`
  が存在する

### 3. try-catch による null 対応の連携 (T7-3 先行確認)
RefreshTypeIds() の try-catch (L37-L51) により、RegistryLocator.ProviderRegistry が null
または未初期化の場合でも _providerTypeIds は System.Array.Empty<string>() にフォールバックされる。
その結果、DrawAvatarProviderSection() は安全に HelpBox 分岐へ分岐する。

## 完了条件の確認
- [x] Registry が空の状態で Inspector を開いた際に Warning HelpBox が表示されること (実装済み)
- [x] 手入力フィールド (Provider Type ID (手入力)) が表示されること (実装済み)
- [x] Registry 未初期化 (null) 時にも NullReferenceException が UI に伝播しないこと (try-catch 経由)

## 結論
T7-1 の完了条件はすべて T3-1 実装時点で達成済み。
本タスクはコード変更を伴わない確認タスクとして OK とする。
