Task: motion-pipeline 8-3 OI-4 slot-core design.md §11.2 の Hide 実装記述更新

【本タスクの位置付け】
- tasks.md §8-3 (OI-4) 「slot-core design.md §11.2 の Hide 実装記述更新」
- 作業内容: slot-core 側 design.md §11.2 の `Hide` 実装記述を `Renderer.enabled = false` (= `GameObject.SetActive(false)` ではない) に更新することを slot-core 担当へ依頼し、整合状態を記録する
- 本 Spec (motion-pipeline) の確定仕様は `Renderer.enabled = false` (design.md §8.4)
- motion-pipeline 側の作業は slot-core 側への通知記録のみ

【slot-core 側の整合状態確認】
- 参照: `.kiro/specs/slot-core/design.md` §11.2 「重要な実装制約」 (L1700 付近)
- 現在の記述:
    「`Hide`: アバターに紐付く全 `Renderer` コンポーネントの `enabled = false` にする。
     **`GameObject.SetActive(false)` は使用しない** (motion-pipeline の確定実装と統一)。
     GameObject は生存させる。エラー解消後の次フレームの正常 Apply 時に
     `Renderer.enabled = true` に復元する。」
- ⇒ motion-pipeline design.md §8.4 (「`Renderer.enabled = false`」「GameObject 自体は生存させる」
  「次フレームの `Apply()` が例外なく正常完了した場合に `Renderer` を再有効化する」) と
  全項目一致している。
- Wave 2 slot-core 実装フェーズ (`_impl_progress.txt` Wave 2 完了 56/56) で既に反映済み。

【motion-pipeline 側の修正】
- design.md §8.4 末尾の注記が「slot-core design.md の 11.2 章では `Hide` の実装として
  `GameObject.SetActive(false)` が言及されている」という旧状態を示していたため、
  slot-core 側が整合済みである現状を反映するよう更新した。
- 新記述: 「`Hide` の確定実装は `Renderer.enabled = false` とし、`GameObject.SetActive(false)`
  は使用しない。…slot-core design.md §11.2 も同仕様に整合済み…両 Spec 間の Hide 実装記述は
  一致している (OI-4 対応)」

【slot-core 担当への通知内容 (記録)】
- 依頼背景: motion-pipeline design.md §8.4 にて Hide 実装を `Renderer.enabled = false` に確定
  (Req 12 AC4 準拠: GameObject 破棄禁止、次フレームの正常 Apply で復帰)。
- 依頼事項: slot-core design.md §11.2 「Fallback 挙動」の `Hide` 項目を
  `Renderer.enabled = false` 基準で明記し、`GameObject.SetActive(false)` を使用しない旨を
  示すこと。
- 対応状況: 依頼済み/反映済み (上記参照)。追加アクション不要。

【結論】
- slot-core design.md §11.2 は motion-pipeline 確定仕様と整合済みであることを確認。
- motion-pipeline design.md §8.4 注記を最新状態に合わせて更新。
- OI-4 クローズ。
