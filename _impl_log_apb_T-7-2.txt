Task: avatar-provider-builtin T-7-2 BuiltinAvatarProviderInstantiateTests シナリオ Y インスタンス化テストを実装する

## 実装内容

- 既存の `BuiltinAvatarProviderInstantiateTests.cs` に `RequestAvatar_ScenarioY_InstantiatesPrefab` テストメソッドを追記した。
- シナリオ Y (design.md §4) はランタイム動的生成経路:
  - `ScriptableObject.CreateInstance<BuiltinAvatarProviderConfig>()` で Config を生成する。
  - `config.avatarPrefab` に `GameObject.CreatePrimitive(PrimitiveType.Cube)` のオブジェクトを代入する。
- 検証項目:
  1. `RequestAvatar()` が null でない GameObject を返すこと (Req 3 AC 1)。
  2. Unity 生存判定 (operator true) で true であること。
  3. Prefab 参照そのものではなく `Object.Instantiate` の複製であること。
  4. `GameObject.scene.IsValid()` により Scene 上に配置されていること (Req 3 AC 2)。
  5. `Resources.FindObjectsOfTypeAll<GameObject>()` で列挙可能であること (Req 9 AC 3)。
- シナリオ X と同一の結果 (null でない Scene 上の GameObject / Prefab 複製) が得られることをもって、
  Factory が生成経路に依存しない一貫した挙動を示すことを検証する (tasks.md T-7-2 明示要件)。

## 変更ファイル

- `RealtimeAvatarController/Packages/com.cysharp.realtimeavatarcontroller/Tests/PlayMode/avatar-provider-builtin/BuiltinAvatarProviderInstantiateTests.cs`
  - クラス summary を T-7-1 / T-7-2 両対応に更新。
  - Scenario Y 用フィールド (`_scenarioYPrefab` / `_scenarioYConfig`) を追加。
  - `TearDown` で Scenario Y 用リソースも DestroyImmediate するよう追記。
  - `RequestAvatar_ScenarioY_InstantiatesPrefab` テストメソッドを追加。

## Requirements カバレッジ

- Req 2 AC 3 (ランタイム動的生成 Config で Prefab をセットできる)
- Req 3 AC 1 (RequestAvatar が Prefab をインスタンス化する)
- Req 9 AC 3 (PlayMode でシナリオ X/Y 両方を検証する)

## 非対応 / 次タスク

- T-7-3 以降の null Prefab・ライフサイクル PlayMode テストは本タスク範囲外。
