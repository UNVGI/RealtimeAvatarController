Task: 12.6 ApplyFailure とフォールバック処理のスケルトンを TDD で実装する

# TDD サイクル
1. Red: `SlotManagerTests.cs` に ApplyWithFallback の期待動作テストを先に追加。
   - ApplyWithFallback_ActionSucceeds_ExecutesActionAndPublishesNoError
   - ApplyWithFallback_HoldLastPoseOnException_PublishesApplyFailureAndDoesNotModifyAvatar
   - ApplyWithFallback_HideOnException_DisablesAllRenderersAndKeepsGameObjectActive
   - ApplyWithFallback_TPoseOnException_PublishesApplyFailure (スケルトン確認)
   - ApplyWithFallback_UnknownSlotId_DoesNothingAndPublishesNoError
   - ApplyWithFallback_ExceptionDoesNotPropagateToCaller

2. Green: `SlotManager.cs` に以下を実装。
   - `internal void ApplyWithFallback(string slotId, Action applyAction)`
     * applyAction / slotId / resources のガード → no-op
     * applyAction 正常完了 → 何もせず戻る
     * 例外時 → ExecuteFallback + ISlotErrorChannel.Publish(ApplyFailure)
     * fallback 中の二次例外は握り潰し Debug.LogWarning
     * applyAction 例外は呼び出し側に伝播しない
   - `private static void ExecuteFallback(FallbackBehavior, GameObject)`
     * HoldLastPose: no-op (Req 13.3)
     * TPose: スケルトン (motion-pipeline 合意後に Humanoid リセット実装予定)
     * Hide: GetComponentsInChildren<Renderer>(true) で全 Renderer.enabled = false
       ※ GameObject.SetActive(false) は使用しない (validation-design.md 引き継ぎ事項 #3 / §11.2)

3. Mock 補強: `MockAvatarProvider.AvatarFactory` を追加し、Hide テスト等でテスト側が
   Renderer 付きの具体 GameObject を返せるようにした (既存 RequestAvatarFunc /
   RequestAvatarAsyncFunc の振る舞いは維持)。

# 設計的決定
- ApplyWithFallback は internal スコープ (タスク要件「内部メソッド」)。
- 未登録 slotId / _resources 未保有 slotId は no-op で安全側に倒す (ErrorChannel にも発行しない)。
- Disposed 状態の SlotManager は no-op (ThrowIfDisposed ではなく静かに戻る)。
- Hide の自動回復 (次フレーム Renderer.enabled = true 復元) は Req 13.5 未確定のため未実装
  (validation-design.md 引き継ぎ事項 #8 / motion-pipeline 合意後 / タスク 15.5 で別途コメント整備)。
- TPose の Humanoid リセット具体 API は motion-pipeline 合意後に差し替え。
  TODO コメントを ExecuteFallback 内と設計 XML コメントに記載済み。

# 対応 Requirement
- Req 13.3 (フォールバック挙動分岐)
- Req 13.4 (ApplyFailure の ErrorChannel 発行)
- Req 12.4 (SlotError.Category = ApplyFailure)
