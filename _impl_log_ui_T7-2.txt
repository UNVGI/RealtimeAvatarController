T7-2 MoCap Source Registry 未初期化時の HelpBox 実装

## 検証内容

### 1. T7-2 完了条件の再確認
- Registry に MoCapSource が登録されていない (`_moCapSourceTypeIds.Length == 0`、結果として `options == null`)
  状態で Inspector を開いた際に、Warning HelpBox と Source Type ID 手入力フィールドが表示されることを保証する。

### 2. 既存実装の確認 (T4-1 で実装済み)
ファイル: `Packages/com.cysharp.realtimeavatarcontroller/Samples~/UI/Editor/SlotSettingsEditor.cs`
DrawMoCapSourceSection() 内 (L116-L126) に T7-2 で指定されたコードスニペットと
完全一致する実装が既に存在する:

```csharp
string[] options = _moCapSourceTypeIds.Length > 0
    ? new[] { "(未割り当て)" }.Concat(_moCapSourceTypeIds).ToArray()
    : null;

if (options == null)
{
    EditorGUILayout.HelpBox(
        "Registry に MoCapSource が未登録です。\n[InitializeOnLoadMethod] が実行されているか確認してください。",
        MessageType.Warning);
    EditorGUILayout.PropertyField(typeIdProp, new GUIContent("Source Type ID (手入力)"));
}
else
{
    // (未割り当て) + 登録 typeId のドロップダウン描画
}
```

検証項目:
- [x] `options == null` の分岐が存在する (L120)
- [x] HelpBox メッセージがタスク指定の文字列と一致する
  ("Registry に MoCapSource が未登録です。\n[InitializeOnLoadMethod] が実行されているか確認してください。")
- [x] `MessageType.Warning` を使用している
- [x] 手入力フォールバック `EditorGUILayout.PropertyField(typeIdProp, new GUIContent("Source Type ID (手入力)"))`
  が存在する

### 3. try-catch による null 対応の連携 (T7-3 先行確認)
RefreshTypeIds() の try-catch (L37-L51) により、RegistryLocator.MoCapSourceRegistry が null
または未初期化の場合でも _moCapSourceTypeIds は System.Array.Empty<string>() にフォールバックされる。
その結果、DrawMoCapSourceSection() は安全に options == null 分岐へ分岐する。

## 完了条件の確認
- [x] MoCap Source Registry が空の状態で Inspector を開いた際に Warning HelpBox が表示されること (実装済み)
- [x] 手入力フィールド (Source Type ID (手入力)) が表示されること (実装済み)
- [x] Registry 未初期化 (null) 時にも NullReferenceException が UI に伝播しないこと (try-catch 経由)

## 結論
T7-2 の完了条件はすべて T4-1 実装時点で達成済み。
本タスクはコード変更を伴わない確認タスクとして OK とする。
